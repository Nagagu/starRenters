import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ff00c8", // Color azul predeterminado
    },
    secondary: {
      main: "#ff4081", // Color rosado
    },
    white: {
      main: "#ffffff",
    },
    basic: {
      main: "#1e3c72",
    },
  },
});

export default theme;
