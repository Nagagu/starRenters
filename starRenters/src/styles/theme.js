import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2", // Color azul predeterminado
    },
    secondary: {
      main: "#ff4081", // Color rosado
    },
  },
});

export default theme;
